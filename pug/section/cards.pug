#card_all.menuCards 
    each _ in Array(12)
        .cardLoader.shimmer 
#card_country.menuCards 
    each _ in Array(12)
        .cardLoader.shimmer 
#card_city.menuCards 
    each _ in Array(12)
        .cardLoader.shimmer 
#card_flag.menuCards
    each _ in Array(12)
        .cardLoader.shimmer 
#card_game.menuCards 
    each _ in Array(12)
        .cardLoader.shimmer